# Database configuration fallback.
# Preferred for deployment: set environment variables DB_URL, DB_USER, DB_PASSWORD.
# This file is used only when those environment variables are not present.

# Supabase/PostgreSQL JDBC URL example:
# db.url=jdbc:postgresql://db.<project-ref>.supabase.co:5432/postgres?sslmode=require

db.url=jdbc:postgresql://localhost:5432/ration_db
db.user=change_me
db.password=change_me
